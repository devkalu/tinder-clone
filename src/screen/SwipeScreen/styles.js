import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#edf6f9",
    alignItems: "center",
  },
  card: {
    alignItems: "center",
  },
});
